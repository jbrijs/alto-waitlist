import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Alto — AI-Native Autonomous Marketing Agency",
  description:
    "Alto autonomously manages your online presence — SEO, AEO, Google Business Profile, and content — delivering measurable lead growth without agencies or manual effort. Secure early access.",
  openGraph: {
    title: "Alto — AI-Native Autonomous Marketing Agency",
    description:
      "Your AI marketing agency, always on. Secure early access today.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
